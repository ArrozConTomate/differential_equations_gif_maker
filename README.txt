This software creates a .gif file when given an ordinary differential equation (ODE). It is used to visualize how several particles are affected by the ODE.

The 'tornados.gif' file has been created as an example. The ODE used is: x' = sin(x+y); y' = cos(x-y)